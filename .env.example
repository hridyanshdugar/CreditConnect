# JWT Secret Key for token signing
# Generate a strong secret using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# Database Path
# Default: ./data/creditconnect.db (relative to project root)
# You can use an absolute path or relative path
DATABASE_PATH=./data/creditconnect.db
