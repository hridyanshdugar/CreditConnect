import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: File size limits are handled in the route handlers
  // The 'api' configuration option was removed in Next.js 15
};

export default nextConfig;
